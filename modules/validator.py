# validator.py


class StudentValidator:
    # Time for votting
        #If valid Vote
            if Voted
                Redirect to results
            if not Vote
    # redirect to results dashboard


class UserValidator:
